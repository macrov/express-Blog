extends layout

block content
  - each post in posts
    p
      h2
        a(href='#') #{post.title}
    p(class='info')
      |Author:
      a(href='#') #{post.name}
      |<br />
      |Date:
      a #{post.time.minute}
    p
      !{post.post}
